<Window x:Class="Minesweeper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сапер" Height="1000" Width="1000" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <!-- Строка для меню/кнопок (высота подстраивается под содержимое) -->
            <RowDefinition Height="Auto"/>
            <!-- Строка для игрового поля (занимает всё оставшееся место) -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Кнопка перезапуска уровня -->
        <Button Grid.Row="0" Content="Перезапуск уровня" 
                Padding="10,5" Margin="0,0,0,10" 
                HorizontalAlignment="Center" 
                Click="RestartButton_Click" 
                FontWeight="Bold"/>

        <!-- Поле игры -->
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Cells}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="{Binding Rows}" Columns="{Binding Columns}"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button CommandParameter="{Binding}" 
                            Click="Cell_Click" 
                            MouseRightButtonDown="Cell_RightClick"
                            Margin="1"
                            FontWeight="Bold" FontSize="16">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Content" Value=""/>
                                <Setter Property="Background" Value="LightGray"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsFlagged}" Value="True">
                                        <Setter Property="Content" Value="🚩"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsRevealed}" Value="True">
                                        <Setter Property="Background" Value="White"/>
                                        <Setter Property="IsEnabled" Value="False"/>
                                    </DataTrigger>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsRevealed}" Value="True"/>
                                            <Condition Binding="{Binding IsMine}" Value="True"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Content" Value="💣"/>
                                        <Setter Property="Background" Value="Red"/>
                                    </MultiDataTrigger>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsRevealed}" Value="True"/>
                                            <Condition Binding="{Binding IsMine}" Value="False"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Content" Value="{Binding NeighborMines}"/>
                                    </MultiDataTrigger>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsRevealed}" Value="True"/>
                                            <Condition Binding="{Binding NeighborMines}" Value="0"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Content" Value=""/>
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>